<?php

return [
    /*
     * 題材驅動候選。題材本身來自 config('news.transmission')，這裡只放候選的
     * 範圍控制。
     */

    /*
     * 延伸層（與某個核心標的同產業的已快取標的）的輸出上限，用來擋版面與頁面
     * 成本（每檔候選要跑一次營收驗證，實測約 5ms／檔）。
     *
     * 這是**延伸層的總數上限，不是每個方向各 20**。
     */
    'max_extended' => 20,
];
