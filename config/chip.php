<?php

return [

    /*
    |--------------------------------------------------------------------------
    | 籌碼面快取新鮮度
    |--------------------------------------------------------------------------
    |
    | 三大法人買賣超為每日收盤後公佈，盤中不會變動，故 TTL 以「小時」為單位
    | 即可涵蓋一個交易日。抓取失敗改用較短的 failure TTL 節流重試，避免對
    | 故障或達到流量上限的 FinMind 每次開頁重打。
    |
    */

    'ttl_hours' => (int) env('CHIP_TTL_HOURS', 12),
    'failure_ttl_hours' => (int) env('CHIP_FAILURE_TTL_HOURS', 2),

    /*
    |--------------------------------------------------------------------------
    | 預設回溯天數
    |--------------------------------------------------------------------------
    |
    | 日曆日（非交易日），供上游 start_date 使用。60 日約可涵蓋 40 個交易日，
    | 足以觀察外資連續買賣超的波段。
    |
    */

    'history_days' => (int) env('CHIP_HISTORY_DAYS', 60),

];
