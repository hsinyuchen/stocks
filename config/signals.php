<?php

return [

    /*
    |--------------------------------------------------------------------------
    | 強度門檻
    |--------------------------------------------------------------------------
    |
    | 原本 SignalEngine 只看方向：K 高於 D 0.01 與高於 20 同樣計 +1，把雜訊
    | 當成訊號。這些門檻讓「差距太小」落在中性，不計分。
    |
    | 各項用該指標自身的尺度：KD 是 0-100 的量故用絕對差；MACD 柱與均線差的
    | 絕對值隨股價量級變動，故用相對於價格的百分比。
    |
    */

    'kd_gap' => (float) env('SIGNAL_KD_GAP', 2.0),
    'macd_pct' => (float) env('SIGNAL_MACD_PCT', 0.001),
    'ma_bias_pct' => (float) env('SIGNAL_MA_BIAS_PCT', 0.5),

    /*
    |--------------------------------------------------------------------------
    | 波動率狀態門檻（布林帶寬佔中軌的百分比）
    |--------------------------------------------------------------------------
    |
    | 帶寬收斂代表盤整，此時的交叉訊號多為雜訊；擴張代表趨勢成形。
    | 這兩個值是通用起點，未經回測驗證——可用 screener:backtest 檢驗調整後
    | 的規則是否真的產生超額報酬。
    |
    */

    'squeeze_pct' => (float) env('SIGNAL_SQUEEZE_PCT', 8.0),
    'expansion_pct' => (float) env('SIGNAL_EXPANSION_PCT', 20.0),

];
